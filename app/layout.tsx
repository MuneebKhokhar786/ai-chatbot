import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: 'AI Assistant',
  description: "Meet AI Chat Assistant, your intelligent, always-available virtual companion powered by cutting-edge artificial intelligence. Whether you're looking for instant answers, help with writing, learning something new, or just having a casual conversation — AI Chat Assistant is here for you 24/7.",
  icons: {
    icon: '/android-avatar.svg',
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
